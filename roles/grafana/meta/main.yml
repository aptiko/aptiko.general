---

dependencies:

 - role: aptiko.general.website
   website_domain: "{{ grafana_server_name }}"
   website_ssl: letsencrypt
   website_force_ssl: true
   when: '"aptiko.general.webserver" in ansible_role_names and grafana_root_path == "/"'
